package multimonster.common.resource;

import multimonster.common.UniqueIdentifier;

/**
 * A <code>ResourceRequestIdentifier</code> specifies
 * a resource request. It is used for communication
 * between the <code>ResourceManager</code> and the
 * requesting instances.
 * 
 * @author Holger Velke (sihovelk)
 */
public class ResourceRequestIdentifier extends UniqueIdentifier{
	
}
